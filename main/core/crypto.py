import os
from nacl import public, encoding, exceptions as nacl_exceptions
from tkinter import messagebox

def encrypt_file(filepath):
    # Placeholder: Add JWT-based user check if needed
    messagebox.showinfo("Encryption", "Encryption logic here. (User must be authenticated via backend)")

def decrypt_file(filepath):
    # Placeholder: Add JWT-based user check if needed
    messagebox.showinfo("Decryption", "Decryption logic here. (User must be authenticated via backend)")

